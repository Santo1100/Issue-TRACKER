1.ans: var = function-scoped, can be redeclared and can  be hoisted
let= block-scoped, can be reassigned, not hoisted in the same way
const= block-scoped, cannot be reassigned ,but object/array contents can be mutated/modifyed.

2.ans:  It unpacks or spreads) elements of an array or properties of an object into a new array/object or function arguments.

3. ans: map()= creates new array by transforming each element
filter()= creates new array with only elements that pass a condition.
forEach()= executes a function for each element, returns nothing means returns undefined.

4. ans: Shorter syntax for functions (() => {}), does not have its own this, inherits from surrounding scope.

5. ans: String literals using backticks (``) that allow embedded expressions with ${} and multi-line strings without concatenation.
